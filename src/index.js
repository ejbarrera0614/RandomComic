import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import RandomComicApp from './RandomComicApp';

ReactDOM.render(
  <React.StrictMode>
    <RandomComicApp />
  </React.StrictMode>,
  document.getElementById('root')
);
